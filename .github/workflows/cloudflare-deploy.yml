# See https://developers.cloudflare.com/workers/platform/deployments/automate-deployments/

name: Cloudflare Workers Deploy

on:
  push:
    branches:
      - main
    paths:
      # Only deploy when relevant files change
      - 'deploy/cloudflare/**'
      - '.github/workflows/cloudflare-deploy.yml'

jobs:
  deploy:
    name: Deploy Worker to Cloudflare
    runs-on: ubuntu-latest
    concurrency: cloudflare-deploy-group  # Ensure only one deployment runs at a time
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Verify wrangler.toml exists
        run: |
          if [ ! -f "deploy/cloudflare/wrangler.toml" ]; then
            echo "❌ wrangler.toml not found in deploy/cloudflare/"
            exit 1
          fi
          echo "✅ wrangler.toml found"

      - name: Check KV namespace configuration
        working-directory: deploy/cloudflare
        run: |
          if grep -q "YOUR_KV_NAMESPACE_ID" wrangler.toml; then
            echo "⚠️  Warning: KV namespace IDs are placeholders"
            echo "Please create KV namespaces and update wrangler.toml before deploying"
            echo "Run: wrangler kv namespace create 'CACHE'"
            exit 1
          fi
          echo "✅ KV namespace IDs configured"

      - name: Deploy to Cloudflare Workers
        working-directory: deploy/cloudflare
        run: wrangler deploy --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Check deployment status
        working-directory: deploy/cloudflare
        run: wrangler deployments list --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

